import SwiftUI

public struct SettingsView: View {
    public init() {}

    public var body: some View {
        Text("Settings")
            .frame(width: 400, height: 300)
            .padding()
    }
}
